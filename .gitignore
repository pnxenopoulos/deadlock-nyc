# Demos
*.dem

# Dependencies
node_modules

# Build output
dist
.vite
*.tsbuildinfo

# WASM build artifacts (regenerate with `bun run wasm`)
src/wasm/pkg
wasm/target

# Synced game data (regenerated by scripts/sync-game-data.sh; large)
scripts/.vdata/
/abilities.vdata
/heroes.vdata
/modifiers.vdata

# Large local game assets — extracted via Source 2 Viewer, not committed
/deadlock-images/
/panorama/
/deadlock-nyc.zip

# Local env, editor & OS cruft
.env
.env.local
.env.*.local
.DS_Store
.claude/settings.local.json
*.md
